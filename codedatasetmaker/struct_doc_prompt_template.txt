你是一名资深嵌入式软件架构师。

请基于以下信息，为结构体 `{struct_name}` 生成专业文档说明，要求包括：
1. 结构体整体设计意图
2. 各字段的业务语义与数据来源
3. 使用场景与典型生命周期
4. 并发/中断/一致性风险
5. 使用与扩展注意事项

结构体名称: {struct_name}

结构体详细信息:
```json
{struct_info}
```

结构体源代码:
```c
{struct_definition}
```

该结构体定义在文件: {defined_in}

使用该结构体的代码位置:
{used_by_list}

引用该结构体的函数代码:
{referenced_functions}

请严格按照以下格式输出文档，确保每个部分都有详细内容，除了文档外不要有其它任何内容：

## 结构体概述
### 功能描述
描述该结构体的主要功能和作用。

### 字段说明
列出该结构体的所有字段及其含义。

## 设计意图
说明该结构体的设计思路和实现意图。

## 使用场景
描述该结构体的典型使用场景。

## 注意事项
说明使用该结构体时需要注意的事项。
