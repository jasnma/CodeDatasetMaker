你是一名资深嵌入式软件架构师。

请基于以下信息，为全局变量 `{var_name}` 生成专业文档说明，要求包括：
1. 谁可以读 / 谁可以写
2. 在什么时序/上下文中会被访问
3. 它是否安全（并发 / 中断 / 任务）

全局变量名称: {var_name}

全局变量详细信息:
```json
{var_info}
```

该全局变量定义在文件: {defined_in}

使用该全局变量的代码位置:
{used_in_list}

引用该全局变量的函数代码:
{referenced_functions}

请严格按照以下格式输出文档，确保每个部分都有详细内容，除了文档外不要有其它任何内容：

## 全局变量概述
### 变量名称
描述该全局变量的名称。

### 功能描述
描述该全局变量的主要功能和作用。

### 数据类型
说明该全局变量的数据类型及其含义。

## 设计意图
说明该全局变量的设计思路和实现意图。

## 使用场景
描述该全局变量的典型使用场景。

## 并发与一致性说明
描述该全局变量的并发与一致性要求。

## 注意事项
说明使用该全局变量时需要注意的事项。
