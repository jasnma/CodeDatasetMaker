#include <stdio.h>

// 创建一个非常大的文件用于测试大小限制功能
// 这个文件将包含大量重复代码以增加文件大小

// 创建一个非常大的文件用于测试大小限制功能
// 这个文件将包含大量重复代码以增加文件大小
void very_large_function_1() {
    int i;
    for (i = 0; i < 5000; i++) {
        printf("This is a very large function 1, iteration %d\n", i);
    }
}

/** 创建一个非常大的文件用于测试大小限制功能*/
/* 创建一个非常大的文件用于测试大小限制功能*/
void very_large_function_2() {
    int i;
    for (i = 0; i < 5000; i++) {
        printf("This is a very large function 2, iteration %d\n", i);
    }
}

/*
 * 创建一个非常大的文件用于测试大小限制功能
*/
void very_large_function_3() {
    int i;
    for (i = 0; i < 5000; i++) {
        printf("This is a very large function 3, iteration %d\n", i);
    }
}
